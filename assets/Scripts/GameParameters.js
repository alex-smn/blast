const {ccclass, property} = cc._decorator;

export default class {
 	static columns = 12; // N
	static rows = 12; // M
	// static tileParentList = [];
	// static tileItemList = [];

	static tileColors = {
		blue: "Blue",
		green: "Green",
		purple: "Purple",
		red: "Red",
		yellow: "Yellow"
	};
}